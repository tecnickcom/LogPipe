// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("logpipe", "for logpipe support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("logpipe", "enable logpipe support", "no");

if (PHP_LOGPIPE != "no") {
	EXTENSION("logpipe", "logpipe.c");
}

